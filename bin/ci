#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

run() {
  echo
  echo "▶ $*"
  "$@"
}

run npm run format:check
run npm run lint
run npm run typecheck
run npm test
run npm run build

echo
echo "✓ all CI checks passed"
